<div class="bgproductos">
    <div class="container">
        <ol class="breadcrumbs">
            <?php $this->Breadcrumbs->output(); ?>
        </ol>
        <?php echo $this->view->partial('partials/nav'); ?>
        <div style="background: #f7f5f5; padding: 15px; border-top:2px solid #99bf10; border-bottom:2px solid #99bf10;" class="texto-descripcion"><?php echo $categoria->descripcion_principal; ?></div>
        </br>
        <?php foreach ($productos as $producto) { ?>
            <div class="bloque-producto">
                <a target="_blank" onclick="estadisticaProducto(<?php echo $producto->id; ?>)" href="<?php echo $producto->enlace; ?>">
                    <?php 
                        echo $this->tag->image(
                            [
                                $producto->imagen,
                                'alt' => $producto->nombre_producto,
                                'title' => $producto->nombre_producto
                                ]
                            );
                    ?>
                </a>
                <br>
                <p class="descripcion"><?php echo $this->Truncate->truncar(strip_tags($producto->nombre_producto), 75); ?></p>
                <?php $oferta = ($producto->es_rebajado == 1) ? "Oferta " : ""; ?>
                <?php if ($producto->TipoMonedas->id == 1) { ?>
                    <span class="descuento"><?php echo $oferta . $producto->precio; ?>&nbsp;<?php echo $producto->TipoMonedas->tipo; ?></span>
                <?php } else { ?>
                    <span class="descuento"><?php echo $oferta . $producto->TipoMonedas->tipo; ?>&nbsp;<?php echo $producto->precio; ?></span>
                <?php } ?>
                <br>
                <a target="_blank" onclick="estadisticaProducto(<?php echo $producto->id; ?>)" href="<?php echo $producto->enlace; ?>" class="button">+ INFO</a>
            </div>
        <?php } ?>
        <div class="clearfix"></div>
        <div style="background: #f7f5f5; padding: 15px; border-top:2px solid #99bf10; border-bottom:2px solid #99bf10;" class="texto-descripcion"><?php echo $categoria->descripcion_secundaria; ?></div>
        <div class="clearfix"></div>
        <?php if (isset($contenidoRelacionado)) { ?>
            <h2 class="mbottom">También te puede interesar</h2>
            <?php foreach ($contenidoRelacionado as $contenido) { ?>
                <div class="bloque-producto">
                    <a href="/<?php echo $contenido->slug; ?>">
                        <p class="nombre"><?php echo $contenido->nombre; ?></p>
                        <br>
                        <?php
                            echo $this->tag->image(
                                [
                                    $contenido->imagen,
                                    'alt' => $contenido->nombre,
                                    'title' => $contenido->nombre
                                    ]
                                );
                        ?>
                        <p class="descripcion"><?php echo $this->Truncate->truncar(strip_tags($contenido->description_seo), 75); ?></p>
                        <br>
                    </a>
                </div>
            <?php } ?>
        <?php } ?>
        <div class="clearfix"></div>
    </div>
</div>